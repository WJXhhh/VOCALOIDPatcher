<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0-windows</TargetFramework>
        <OutputType>Library</OutputType>
        <PackageId>Microsoft.Xaml.Behaviors.Wpf</PackageId>
        <Title>Microsoft.Xaml.Behaviors.Wpf</Title>
        <Authors>Microsoft</Authors>
        <Description>Easily add interactivity to your apps using XAML Behaviors for WPF. Behaviors encapsulate reusable functionalities for elements that can be easily added to your XAML without the need for more imperative code.</Description>
        <Company>Microsoft Corporation</Company>
        <Product>Microsoft.Xaml.Behaviors</Product>
        <Copyright>© Microsoft Corporation. All rights reserved.</Copyright>
        <EmbedUntrackedSources>true</EmbedUntrackedSources>
        <AllowedOutputExtensionsInPackageBuildOutputFolder>$(AllowedOutputExtensionsInPackageBuildOutputFolder);.pdb</AllowedOutputExtensionsInPackageBuildOutputFolder>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <UseWPF>true</UseWPF>
        <NoWarn>1701;1702;1591</NoWarn>
        <DelaySign>true</DelaySign>
        <Version>1.1.0.0</Version>
        <AssemblyVersion>1.1.0.0</AssemblyVersion>
        <FileVersion>1.1.0.0</FileVersion>
        <Nullable>enable</Nullable>
        <RootNamespace />
        <AssemblyName>Microsoft.Xaml.Behaviors</AssemblyName>
        <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
        <NativePlaybackClockDir>$(MSBuildProjectDirectory)\..\native\playback-clock</NativePlaybackClockDir>
        <NativePlaybackClockManifest>$(NativePlaybackClockDir)\Cargo.toml</NativePlaybackClockManifest>
        <NativePlaybackClockCargo Condition="'$(NativePlaybackClockCargo)' == ''">$(USERPROFILE)\.cargo\bin\cargo.exe</NativePlaybackClockCargo>
        <NativePlaybackClockProfile Condition="'$(Configuration)' == 'Release'">release</NativePlaybackClockProfile>
        <NativePlaybackClockProfile Condition="'$(NativePlaybackClockProfile)' == ''">debug</NativePlaybackClockProfile>
        <NativePlaybackClockCargoArgs Condition="'$(Configuration)' == 'Release'">--release</NativePlaybackClockCargoArgs>
        <NativePlaybackClockOutput>$(NativePlaybackClockDir)\target\$(NativePlaybackClockProfile)\v6patch_clock.dll</NativePlaybackClockOutput>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Lib.Harmony" Version="2.4.2" />
        <PackageReference Include="ILRepack.Lib.MSBuild.Task" Version="2.0.44.2" PrivateAssets="all" />
        <PackageReference Include="System.Text.Encoding.CodePages" Version="8.0.0" />
        <PackageReference Include="ToolGood.Words.Pinyin" Version="3.1.0.4" />
        <PackageReference Include="YamlDotNet" Version="13.7.1" />
        <PackageReference Include="ZstdSharp.Port" Version="0.8.8" />
    </ItemGroup>

    <ItemGroup>
        <Reference Include="VOCALOID6">
            <HintPath>C:\Program Files\VOCALOID6\Editor\VOCALOID6.dll</HintPath>
            <Private>false</Private>
            <ExcludeAssets>runtime</ExcludeAssets>
            <SpecificVersion>False</SpecificVersion>
        </Reference>
    </ItemGroup>

    <Target Name="BuildNativePlaybackClock" BeforeTargets="CoreCompile">
        <Warning Condition="!Exists('$(NativePlaybackClockCargo)')"
                 Text="Rust/Cargo was not found at $(NativePlaybackClockCargo); the managed playback clock fallback will be used." />
        <Exec Condition="Exists('$(NativePlaybackClockCargo)')"
              Command="&quot;$(NativePlaybackClockCargo)&quot; build --manifest-path &quot;$(NativePlaybackClockManifest)&quot; $(NativePlaybackClockCargoArgs)" />
        <Error Condition="Exists('$(NativePlaybackClockCargo)') and !Exists('$(NativePlaybackClockOutput)')"
               Text="Cargo completed without producing $(NativePlaybackClockOutput)." />
        <MakeDir Condition="Exists('$(NativePlaybackClockOutput)')"
                 Directories="$(OutDir)VOCALOIDPatcher\native" />
        <Copy Condition="Exists('$(NativePlaybackClockOutput)')"
              SourceFiles="$(NativePlaybackClockOutput)"
              DestinationFolder="$(OutDir)VOCALOIDPatcher\native"
              SkipUnchangedFiles="true" />
    </Target>

</Project>
